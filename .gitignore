# Saved photo strips (local only)
server/saved/
server/saved/**

# Missions issuance log (party data)
server/data/issuance.json

# Local HTTPS certs (if you generated them)
server/certs/
server/certs/**

# Node / build artifacts
node_modules/
server/node_modules/
dist/
.DS_Store

# Local runbook (contains environment-specific info)
README_LOCAL.md