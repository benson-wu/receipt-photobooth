{
  "name": "receipt-photobooth",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview:netlify": "npm run build && npm run preview",
    "pocha": "caffeinate -i sh -c 'npm run build && cd server && npm start'",
    "qr:missions": "node scripts/missions-qr.js",
    "start:ngrok": "./start-ngrok.sh"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
